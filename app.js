const $ = (s) => document.querySelector(s);
async function load(){const [summary,series]=await Promise.all([fetch("data/summary.json").then(r=>r.json()),fetch("data/series.json").then(r=>r.json())]);$("#device").textContent=`${summary.device} / iOS ${summary.os}`;$("#period").textContent=`${summary.start} → ${summary.end}`;const cards=[["記録時間",`${summary.duration_min} 分`,`${summary.location_samples} location points`],["移動距離",`${(summary.distance_m/1000).toFixed(2)} km`,"位置データから算出"],["平均速度",`${summary.speed_mean_mps.toFixed(2)} m/s`,`${(summary.speed_mean_mps*3.6).toFixed(1)} km/h`],["気圧変化",`${summary.pressure_change_hpa>=0?"+":""}${summary.pressure_change_hpa} hPa`,`${summary.pressure_min_hpa}–${summary.pressure_max_hpa} hPa`]];$("#cards").innerHTML=cards.map(c=>`<div class="card"><div class="label">${c[0]}</div><div class="value">${c[1]}</div><div class="sub">${c[2]}</div></div>`).join("");drawChart("pressureChart",series.pressure,"気圧","hPa");drawChart("speedChart",series.speed,"速度","m/s");drawChart("heightChart",series.height,"高度","m");const pressureDelta=summary.pressure_max_hpa-summary.pressure_min_hpa;$("#analysis").innerHTML=[["最高速度",`${summary.speed_max_mps.toFixed(2)} m/s（${(summary.speed_max_mps*3.6).toFixed(1)} km/h）`],["高度レンジ",`${summary.height_min_m}–${summary.height_max_m} m（差 ${Math.abs(summary.height_max_m-summary.height_min_m).toFixed(1)} m）`],["気圧レンジ",`${summary.pressure_min_hpa}–${summary.pressure_max_hpa} hPa（幅 ${pressureDelta.toFixed(3)} hPa）`],["取得サンプル",`気圧 ${summary.barometer_samples} 点 / 位置 ${summary.location_samples} 点`]].map(x=>`<div class="analysis-row"><span>${x[0]}</span><strong>${x[1]}</strong></div>`).join("")}
function drawChart(id,data,label,unit){const canvas=document.getElementById(id),dpr=window.devicePixelRatio||1,rect=canvas.getBoundingClientRect(),w=Math.max(320,rect.width),h=Number(canvas.getAttribute("height"))||240;canvas.width=w*dpr;canvas.height=h*dpr;const ctx=canvas.getContext("2d");ctx.scale(dpr,dpr);ctx.clearRect(0,0,w,h);const pad={l:46,r:16,t:16,b:30},xs=data.map(p=>p.t),ys=data.map(p=>p.v),xmin=Math.min(...xs),xmax=Math.max(...xs),ymin=Math.min(...ys),ymax=Math.max(...ys),yr=(ymax-ymin)||1,x=t=>pad.l+(t-xmin)/(xmax-xmin||1)*(w-pad.l-pad.r),y=v=>h-pad.b-(v-ymin)/yr*(h-pad.t-pad.b);ctx.font="11px -apple-system, sans-serif";ctx.strokeStyle="#e8ecf0";ctx.fillStyle="#7a8590";ctx.lineWidth=1;for(let i=0;i<4;i++){const yy=pad.t+i*(h-pad.t-pad.b)/3;ctx.beginPath();ctx.moveTo(pad.l,yy);ctx.lineTo(w-pad.r,yy);ctx.stroke();const val=ymax-(ymax-ymin)*i/3;ctx.fillText(val.toFixed(unit==="hPa"?2:1),6,yy+4)}ctx.fillText(`${xmin.toFixed(0)}s`,pad.l,h-8);ctx.fillText(`${xmax.toFixed(0)}s`,w-pad.r-32,h-8);ctx.beginPath();data.forEach((p,i)=>{const xx=x(p.t),yy=y(p.v);i?ctx.lineTo(xx,yy):ctx.moveTo(xx,yy)});ctx.strokeStyle="#1f2937";ctx.lineWidth=2;ctx.stroke();const last=data[data.length-1];ctx.fillStyle="#1f2937";ctx.beginPath();ctx.arc(x(last.t),y(last.v),3.5,0,Math.PI*2);ctx.fill()}
load().catch(err=>{console.error(err);document.body.insertAdjacentHTML("beforeend",`<p style="padding:20px">データの読み込みに失敗しました。GitHub PagesなどHTTP環境で開いてください。</p>`)})
